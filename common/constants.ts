// namespaces
// Clients connects to different namespace based on their types.
export const CONTROLLER_NS = '/controller';
export const DISPLAY_NS = '/display';
export const CHAT_NS = '/chat';

export const PORT = 3000;
export const SERVER = `http://localhost:${PORT}`;
