scripts:
    default:
        DEBUG=debug,psl:* PORT=3000 NODE_ENV=development ts-node --project demo.tsconfig.json
        server.ts --unhandled-rejections=strict
    clean: rm -rf ./lib ./dist
    ts-check: tsc
    build: rollup --config --silent
