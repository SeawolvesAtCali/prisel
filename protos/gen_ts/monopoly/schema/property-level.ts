// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

export class PropertyLevel {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
__init(i:number, bb:flatbuffers.ByteBuffer):PropertyLevel {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPropertyLevel(bb:flatbuffers.ByteBuffer, obj?:PropertyLevel):PropertyLevel {
  return (obj || new PropertyLevel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPropertyLevel(bb:flatbuffers.ByteBuffer, obj?:PropertyLevel):PropertyLevel {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PropertyLevel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

cost():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

rent():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startPropertyLevel(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addCost(builder:flatbuffers.Builder, cost:number) {
  builder.addFieldInt32(0, cost, 0);
}

static addRent(builder:flatbuffers.Builder, rent:number) {
  builder.addFieldInt32(1, rent, 0);
}

static endPropertyLevel(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPropertyLevel(builder:flatbuffers.Builder, cost:number, rent:number):flatbuffers.Offset {
  PropertyLevel.startPropertyLevel(builder);
  PropertyLevel.addCost(builder, cost);
  PropertyLevel.addRent(builder, rent);
  return PropertyLevel.endPropertyLevel(builder);
}
}
