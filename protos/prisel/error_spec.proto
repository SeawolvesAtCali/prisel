syntax = "proto3";

package prisel;

import "prisel/annotations.proto";

message ErrorPayload {
    option (for_system_action) = {
        type: ERROR,
        packet_type: DEFAULT
    };
    
    string message = 1;
    string detail = 2;
}